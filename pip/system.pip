# Python 3 baseline packages for day-to-day system use.
# Use with a virtual environment:
#   python3 -m venv .venv && source .venv/bin/activate
#   pip install -r pip/system.pip

pip>=25.0
setuptools>=75.0
wheel>=0.45

ipython>=8.18




virtualenv>=20.28
